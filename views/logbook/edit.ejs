<%- include('../partials/head') %>
<%- include('../partials/navbar') %>
<div class="mb-6">
  <h2 class="text-2xl font-bold">Edit Logbook</h2>
</div>

<% if (errors.length > 0) { %>
<div class="mb-4 p-3 rounded-md bg-destructive/15 text-destructive border border-destructive/30 text-sm">
  <ul class="list-disc pl-4"><% errors.forEach(e => { %><li><%= e %></li><% }) %></ul>
</div>
<% } %>

<div data-slot="card" class="rounded-xl border bg-card shadow-sm p-6 max-w-2xl">
  <form action="/logbook/<%= logbook.id %>/edit" method="POST" enctype="multipart/form-data" class="flex flex-col gap-4">
    <div class="flex flex-col gap-1.5">
      <label class="text-sm font-medium">Tanggal <span class="text-destructive">*</span></label>
      <input type="date" name="progress_date" required value="<%= new Date(logbook.progress_date).toISOString().split('T')[0] %>"
        class="h-9 rounded-md border border-input bg-background px-3 text-sm outline-none focus-visible:border-ring"/>
    </div>
    <div class="flex flex-col gap-1.5">
      <label class="text-sm font-medium">Kegiatan <span class="text-destructive">*</span></label>
      <textarea name="description" rows="6" required
        class="rounded-md border border-input bg-background px-3 py-2 text-sm outline-none focus-visible:border-ring resize-none"><%= logbook.description %></textarea>
    </div>
    <div class="flex flex-col gap-1.5">
      <label class="text-sm font-medium">Ganti Dokumentasi (opsional)</label>
      <input type="file" name="attachment" accept=".pdf,.png,.jpg,.jpeg,.doc,.docx"
        class="h-9 rounded-md border border-input bg-background px-3 py-1.5 text-sm outline-none focus-visible:border-ring"/>
      <% if (logbook.attachment) { %>
      <p class="text-xs text-muted-foreground">File saat ini: <a href="/uploads/<%= logbook.attachment %>" class="text-primary underline" target="_blank"><%= logbook.attachment %></a></p>
      <% } %>
    </div>
    <div class="flex gap-2 pt-2">
      <button type="submit" class="inline-flex items-center justify-center rounded-md text-sm font-medium bg-primary text-primary-foreground hover:bg-primary/90 h-9 px-4 transition-all">Simpan</button>
      <a href="/logbook" class="inline-flex items-center justify-center rounded-md text-sm font-medium border border-input bg-background hover:bg-accent h-9 px-4 transition-all">Batal</a>
    </div>
  </form>
</div>
<%- include('../partials/footer') %>
